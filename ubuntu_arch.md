| Feature | Arch Linux | Ubuntu |
|---------|------------|--------|
| **Enable MOTD** | `sudo chmod +x /etc/profile.d/motd.sh` | Usually enabled by default |
| **Edit Header** | `sudo nano /etc/update-motd.d/00-header` | `sudo nano /etc/update-motd.d/00-header` |
